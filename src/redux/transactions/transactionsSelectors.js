export const selectTransactions = (s) => s.transactions.items;
export const selectTransactionsLoading = (s) => s.transactions.isLoading;
export const selectTransactionsError = (s) => s.transactions.error;

export const selectCategories = (s) => s.transactions.categories;

export const selectBalance = (s) => s.transactions.balance;
